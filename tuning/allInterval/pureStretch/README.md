# Tuning - all-interval tuning schemes - pure-stretched intervals

Support for all-interval tuning schemes with pure-stretched intervals (e.g. POTE, POTOP).

This module also requires `tuning/pureStretch`.
